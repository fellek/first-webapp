<?xml version="1.0"?>
<ruleset name="App">
    <description>PHP CodeSniffer Konfiguration</description>

    <!-- Zu prüfende Verzeichnisse -->
    <file>app</file>
    <file>config</file>
    <file>database</file>
    <file>routes</file>
    <file>tests</file>

    <!-- Ausschlüsse -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/cache/*</exclude-pattern>

    <!-- Standard: PSR-12 -->
    <rule ref="PSR12"/>

    <!-- PHP-Version -->
    <config name="php_version" value="80300"/>

    <!-- Zeilenlänge: Warnung bei 120, Fehler bei 160 -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="160"/>
        </properties>
    </rule>

    <!-- Ausgabe-Format -->
    <arg name="colors"/>
    <arg value="sp"/>
</ruleset>
